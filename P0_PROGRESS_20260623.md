# RelayCheck Desktop P0 Progress - 2026-06-23

## Status

P0 build baseline restored.

## Changes

- Backed up the broken frontend entry to:
  `relaycheck-desktop/frontend/src/_archive/main.tsx.pre-restore-20260623-214254`
- Replaced the corrupted monolithic `frontend/src/main.tsx` with a clean Recovery Console entry.
- Added `frontend/src/recovery.css`.
- Temporarily narrowed `frontend/tsconfig.json` to `["src/main.tsx"]` so known-corrupted legacy frontend modules do not block the P0 build.
- Generated `frontend/dist` successfully.
- Built `dist/relaycheck.exe` successfully.

## Verification

| Command | Result |
|---|---|
| `npm run build` in `relaycheck-desktop/frontend` | PASS |
| `go test -mod=vendor ./...` in `relaycheck-desktop` | PASS |
| `go build -buildvcs=false -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` | PASS |

## Notes

- `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` fails without `-buildvcs=false` because the workspace has invalid Git/VCS metadata.
- The new frontend is intentionally a P0 recovery baseline, not the full historical UI.
- The old frontend modules under `frontend/src/components`, `frontend/src/lib`, `frontend/src/hooks`, `frontend/src/api`, and `frontend/src/types` still contain known CJK corruption and should be repaired or rebuilt incrementally in M1 before widening `tsconfig.json`.

## Next

1. Fix or reinitialize Git/VCS metadata so normal Go VCS stamping works.
2. Start M1 by rebuilding one page/module at a time behind the Recovery Console baseline.
3. After each clean module lands, widen `tsconfig.json` incrementally and run `npm run build`.

## Update - 2026-06-23 22:05 +08:00

P0 is now verified beyond build-only status.

### Changes

- Added root `.gitignore` to exclude local agent state, caches, runtime data, databases, keys, `node_modules`, and build outputs.
- Reinitialized the empty root `.git` directory with `git init -b main`.
- Confirmed normal Go VCS stamping works again: `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` now passes without `-buildvcs=false`.
- Expanded `relaycheck-desktop/frontend/tsconfig.json` from the Recovery Console entry only to all active `src/**/*.ts` and `src/**/*.tsx` files, with `src/_archive/**` excluded.
- Removed the temporary M1 TypeScript check config after the main `tsconfig.json` covered the active source tree.
- Regenerated `frontend/dist` and rebuilt `dist/relaycheck.exe`.

### Verification

| Command / Check | Result |
|---|---|
| `git status --short` at `E:\zidqiandao` | PASS |
| `git check-ignore` for runtime keys, data, node modules, dist | PASS |
| `npm run build` in `relaycheck-desktop/frontend` | PASS |
| `go test -mod=vendor ./...` in `relaycheck-desktop` | PASS |
| `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` | PASS |
| Runtime `/api/health` | PASS: all checks ok |
| Runtime `/api/auth/session` before login | PASS: unauthenticated |
| Runtime `/` embedded frontend | PASS: HTTP 200 with React root |
| Runtime login + `/api/system/status` | PASS |

### Current State

- The app is runnable from `relaycheck-desktop/dist/relaycheck.exe`.
- Active frontend TypeScript/TSX source now compiles under the main config.
- The UI entry is still the clean Recovery Console, not the full historical operations console.
- Archived historical entries remain under `relaycheck-desktop/frontend/src/_archive/` for future feature restoration.

### Next

1. Restore the full application shell behind the now-green component tree.
2. Use browser screenshots to verify visible CJK text and layout quality after the full shell is reconnected.
3. Add an automated browser smoke test for desktop and mobile widths.
4. Run a secret scan, then create an initial Git snapshot if the staged set is clean.

## Update - 2026-06-23 22:18 +08:00

M1 recovery work continued.

### Changes

- Refactored `frontend/src/main.tsx` to use the shared `src/api/client.ts` request helper.
- Refactored `frontend/src/main.tsx` to use shared frontend types from `src/types/index.ts` instead of local duplicate payload types.
- Kept the Recovery Console behavior intact while aligning the entry with the recovered component/type tree.
- Refined `frontend/src/recovery.css` for a denser operations-console feel:
  - removed decorative radial background highlights,
  - reduced card/button/input radii to 8px,
  - reduced oversized shadows and spacing.
- Added `.pipeline/test-results/` to `.gitignore` so Playwright screenshots remain verification artifacts, not source candidates.

### Verification

| Command / Check | Result |
|---|---|
| `npm run build` in `relaycheck-desktop/frontend` | PASS |
| `go test -mod=vendor ./...` in `relaycheck-desktop` | PASS |
| `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` | PASS |
| Browser smoke with system Chrome, desktop 1365x768 | PASS |
| Browser smoke with system Chrome, mobile 390x844 | PASS |
| Browser console/page errors | PASS: none |
| Horizontal overflow check | PASS: none on desktop or mobile |

### Artifacts

- `relaycheck-desktop/.pipeline/test-results/app-shell-desktop-refined.png`
- `relaycheck-desktop/.pipeline/test-results/app-shell-mobile-refined.png`

### Next

1. Replace the remaining hand-built tab bodies with the recovered domain components where doing so adds real functionality.
2. Add a committed browser smoke script instead of keeping the test only as an ad hoc Playwright run.
3. Fix or replace any visible mojibake in component-level CJK labels after those components are mounted.
4. Prepare a clean initial Git snapshot after a focused secret scan.

## Update - 2026-06-23 22:36 +08:00

Browser smoke is now repeatable from the project.

### Changes

- Added `playwright@1.61.0` as a frontend dev dependency.
- Installed it with browser downloads skipped; the smoke script uses system Chrome or Edge by default.
- Added `frontend/scripts/smoke.mjs`.
- Added `npm run smoke`.
- Documented the smoke command in `relaycheck-desktop/README.md`.
- The smoke script requires `RELAYCHECK_SMOKE_PASSWORD` at runtime and does not store the local password in source.

### Verification

| Command / Check | Result |
|---|---|
| `node --check scripts\smoke.mjs` | PASS |
| `npm run build` | PASS |
| `npm audit --audit-level=low` | PASS: 0 vulnerabilities |
| `go test -mod=vendor ./...` | PASS |
| `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` | PASS |
| `npm run smoke` against local desktop server | PASS |

### Smoke Coverage

- Logs in through the local UI.
- Waits for the Dashboard heading.
- Captures desktop and 390px mobile screenshots.
- Fails on browser console/page errors.
- Fails on horizontal overflow.
- Fails if the shell does not render core RelayCheck text, active navigation, or enough cards.

### Next

1. Mount one recovered domain component at a time behind the smoke gate.
2. Start with read-heavy surfaces: settings diagnostics, account list, then channels.
3. After each component mount, run `npm run build`, `go test`, `go build`, and `npm run smoke`.

## Update - 2026-06-23 23:05 +08:00

Recovered domain components are now mounted behind the smoke gate.

### Changes

- Mounted the recovered `Settings` component in the main app shell.
- Mounted account domain components:
  - `AccountInsights`
  - `AccountForm`
  - `AccountCard`
  - `AccountDetailContent`
- Mounted channel domain components:
  - `ChannelTable`
  - `useChannelActions`
  - `useChannelFilters`
- Mounted `HubRadar` on the Dashboard and added the supporting API reads:
  - `/api/system/diagnostics`
  - `/api/system/action-center`
  - `/api/models/overview`
  - `/api/models/pricing`
  - `/api/usage/overview`
- Expanded the browser smoke script to click every primary tab.
- Removed the prefilled local password from the login form.
- Added shared CSS coverage for recovered components, including:
  - status icons,
  - Settings cards and forms,
  - account cards/forms/insights/drawer,
  - channel cards/filters/drawer,
  - Dashboard Hub Radar.

### Verification

| Command / Check | Result |
|---|---|
| `npm run build` | PASS |
| `go test -mod=vendor ./...` | PASS |
| `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` | PASS |
| `npm run smoke` | PASS |
| Focused secret scan for common token/private-key/default-password patterns | PASS |
| Desktop screenshots for Dashboard, Settings, Accounts, Channels | PASS |
| 390px screenshots for Settings, Accounts, Channels | PASS |

### Notes

- The active app shell is still a recovery shell, but its Dashboard, Settings, Accounts, and Channels surfaces now use recovered domain components.
- The remaining hand-built surfaces are Sites, Check-ins, and Notifications. They are read-heavy and currently pass the smoke gate.
- `frontend/src/_archive/**`, runtime data, build outputs, node modules, and screenshot artifacts remain excluded from active TypeScript/build or Git candidate paths.

### Next

1. Add focused browser assertions for Accounts and Channels, not just screenshots.
2. Decide whether Sites, Check-ins, and Notifications should stay as lightweight recovery views or be rebuilt as richer domain components.
3. Run final dependency/security/build/smoke sweep.
4. Create a scoped Git baseline for `relaycheck-desktop` after final scan.

## Update - 2026-06-23 23:09 +08:00

Final recovery sweep completed for the active shell.

### Changes

- Added focused smoke DOM assertions for:
  - `.account-card` on Accounts,
  - `.channel-card` on Channels,
  - `.settings-grid` on Settings.
- Re-ran the final quality gate after mounting Dashboard, Settings, Accounts, and Channels recovered components.

### Final Verification

| Command / Check | Result |
|---|---|
| `node --check scripts\smoke.mjs` | PASS |
| `npm run build` | PASS |
| `npm audit --audit-level=low` | PASS: 0 vulnerabilities |
| `go test -mod=vendor ./...` | PASS |
| `go build -mod=vendor -ldflags="-H windowsgui" -o dist\relaycheck.exe .` | PASS |
| `npm run smoke` | PASS |
| Focused secret scan | PASS: no matches |
| Residual `relaycheck.exe` process check | PASS: no process left running |
| Ignored paths check for runtime/build/test artifacts | PASS |

### Completion State

- P0 build/runtime baseline is restored.
- Main TypeScript coverage includes all active frontend source and excludes archives.
- Go VCS stamping is restored by valid Git initialization.
- Recovered components now mounted:
  - Dashboard Hub Radar,
  - Settings,
  - Accounts,
  - Channels.
- Lightweight recovery views remain for:
  - Sites,
  - Check-ins,
  - Notifications.
- Browser smoke is repeatable and covers every primary tab.

### Follow-up Candidate

Create a scoped initial Git commit for:

- `.gitignore`
- `P0_PROGRESS_20260623.md`
- `relaycheck-desktop/**`

Excluded by `.gitignore`:

- runtime data,
- keys,
- databases,
- `dist`,
- `node_modules`,
- browser screenshots,
- test result artifacts.

## Update - 2026-06-23 23:40 +08:00

Git baseline hardening completed before the initial commit candidate.

### Changes

- Excluded `frontend/src/_archive/**` from Git so damaged recovery snapshots remain local-only.
- Removed plaintext bootstrap password references from handoff/progress documentation.
- Changed fresh-database admin bootstrap behavior:
  - prefer `RELAYCHECK_BOOTSTRAP_PASSWORD` when present,
  - otherwise generate a local password in `data/bootstrap-admin-password.txt`,
  - keep existing databases unchanged because admin creation is skipped when the user already exists.
- Added tests for environment-provided and generated bootstrap admin passwords.

### Verification Targets

- Secret scan should no longer match the old fixed bootstrap password in staged source/docs.
- Runtime data, keys, databases, build outputs, dependencies, screenshots, and recovery archives remain outside the Git baseline.
